// Import getters for minerals, facilities, transient state
import {
  getMinerals,
  getFacilities,
  getTransientState,
  purchaseMineral,
  setColonyMineral,
} from "./database.js";

// Store the value selected in facility minerals
// We need Purchase Mineral Button
// click event to check name attribute on display in ColonyMinerals
//

// Write and export (to Exomine) a function that returns a string of html
